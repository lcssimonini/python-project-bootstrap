services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    command: uv run uvicorn {{ package_name }}.api.main:app --reload --host 0.0.0.0 --port 8000
