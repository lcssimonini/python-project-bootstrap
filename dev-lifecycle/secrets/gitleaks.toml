# Gitleaks configuration — extends the built-in rules.
# See: https://github.com/gitleaks/gitleaks#configuration

title = "python-project-bootstrap gitleaks config"

# Paths to ignore (test fixtures, lock files, etc.)
[allowlist]
  paths = [
    '''\.hypothesis/''',
    '''uv\.lock''',
    '''\.env\.example''',
  ]
